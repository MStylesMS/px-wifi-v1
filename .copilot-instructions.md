# px-wifi-v1 — AI Instructions

## Purpose

**Generic WiFi Prop Controller** — A battery-powered, WiFi-connected prop controller for escape rooms. Uses an ESP32-S3 with configurable I/O via wiring harness. v1.0 firmware implements a **wire defusal puzzle** with countdown timer, buzzer, and 7-segment display.

## Shared Component Library

Reusable WiFi/MQTT/JSON/battery-monitor code lives in the sibling
`../px-components` repo (`svc_wifi`, `svc_mqtt`, `lib_json_helper`, etc.) and
is pulled in via `EXTRA_COMPONENT_DIRS`. Coding, API, and documentation
conventions for that shared code are defined in
[`../px-components/CODING_STANDARDS.md`](../px-components/CODING_STANDARDS.md) —
read it before adding new shared-worthy code, and prefer extending an
existing `px-components` module over duplicating logic here.

`PX_COMPONENTS_VERSION` (repo root) records the px-components version this
project was last built/validated against. Update it whenever you pick up a
newer px-components release.

## Key Documents

Always read these before making changes:

- `docs/product-brief.md` — Marketing-level feature overview
- `docs/functional-spec.md` — **Primary reference.** Game states, MQTT commands/events, I/O behavior, timing, audio feedback, LED indicators.
- `docs/hardware-spec.md` — MCU, pin assignments, peripherals, BOM, connectors.
- `docs/potential-improvements.md` — Security/architecture findings that
  were deliberately deferred (not fixed) during a past review, with the
  reasoning and a suggested approach for later. Read this before doing a
  code/security review so known, deferred issues aren't re-reported as new
  findings. When a review turns up a finding that should be deferred
  rather than fixed immediately, add it here (same format) instead of only
  mentioning it in chat.

## Build & Flash

```bash
# From this directory:
idf.py set-target esp32s3
idf.py build
idf.py -p /dev/cu.usbmodem* flash monitor
```

Or use the ESP-IDF VS Code extension buttons.

## VS Code ESP-IDF Scope

- This directory is an ESP-IDF app project root and should be treated as an independent build context.
- Use this folder's context for Configure/Build/Flash/Monitor operations.
- Keep project-specific extension settings in `px-wifi-v1/.vscode/settings.json` (for example `idf.port`, target-related vars, and tooling paths).
- Do not assume settings from the umbrella `esp32/` root are sufficient for this project.
- If multiple apps are open in one workspace, verify `px-wifi-v1` is the selected ESP-IDF project before running commands.

## Versioning & Changelog

- `version.txt` (repo root) is the single source of truth for the firmware
  version. ESP-IDF reads it automatically at build time and embeds it into
  `esp_app_desc_t.version` (surfaced via `app->version` in `main.c` and the
  `softwareVersion` / `version` fields in the web UI and device-details API).
- Whenever you commit a set of functional changes (not pure typo/formatting
  fixes), bump `version.txt` by `0.01` for incremental/automatic commits
  (e.g. `0.3` -> `0.31`). Only bump by `0.1` (minor) or `1.0` (major) when
  the user explicitly asks for that kind of release.
- Add a matching entry to `CHANGELOG.md` (top of file, newest first) using
  [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) style `### Added
  / Changed / Fixed` sections, dated with the commit date.
- Do the version bump + changelog entry in the same commit as the change
  it describes.

## Serial Port Conflicts

Before flashing, always kill any open serial monitors/terminals that would block the flash tool:

```bash
# Kill any stray serial monitor or esptool processes
pkill -f "idf.py monitor" || true
pkill -f "screen.*usb" || true
pkill -f "minicom" || true
```

If using VS Code integrated terminal with a monitor open, close that terminal tab first.

- `drv_rgb_led` — RGB LED driver
- `board_devkitc1` — Pin definitions (prototype; will switch to `board_px_wifi` for custom PCB)

The `CMakeLists.txt` sets `EXTRA_COMPONENT_DIRS` to `../px-components`. Override with `PX_COMPONENTS_DIR` env var.

## Wire Defusal Puzzle (v1.0)

- **Game states:** IDLE → ARMED → COUNTDOWN → DEFUSED / DETONATED
- **Inputs:** 4 wire GPIOs + lid switch
- **Outputs:** 7-segment display (I2C), piezo buzzer (PWM), status RGB LED
- **MQTT zone:** `wire-defusal` (configurable)
- **Commands:** start, stop, pause, resume, reset, setTime, setSequence, setPenalty
- **Events:** lidOpened, wireDisconnected, defused, detonated, penaltyApplied, stateChanged, timerSync

See `docs/functional-spec.md` for complete details.

## Planned Components (not yet implemented)

- `svc_wifi` — WiFi station + AP fallback
- `svc_mqtt` — MQTT client with auto-reconnect
- `svc_web` — HTTP config server + WebSocket
- `drv_buzzer` — PWM tone generation
- `drv_7seg` — I2C 7-segment display driver
- `puzzle_wire_defusal` — Wire sequence validation and game state machine
